import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {
    color: 'tomato',
    padding: 12,
    borderColor: 'tomato',
    borderWidth: 1,
    marginBottom: 12,
    borderRadius: 10,
  },
});
