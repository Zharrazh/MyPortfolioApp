import {lightColors} from '@rneui/base';
import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {
    width: '100%',
    padding: 12,
  },

  registerTipContainer: {
    marginTop: 24,
  },

  registerTipText: {
    fontSize: 18,
  },

  registerLink: {
    color: lightColors.primary,
    textDecorationLine: 'underline',
  },

  or: {
    alignSelf: 'center',
  },
});
