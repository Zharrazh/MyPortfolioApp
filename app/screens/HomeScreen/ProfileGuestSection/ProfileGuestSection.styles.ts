import {lightColors} from '@rneui/base';
import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {
    overflow: 'hidden',
  },
  avatar: {
    backgroundColor: lightColors.primary,
  },

  subtitle: {fontSize: 12, color: lightColors.grey2},
});
